name: Cursor GitHub App

on:
  repository_dispatch:
    types: [cursor-background-agent]
  workflow_dispatch:

jobs:
  cursor-app:
    name: Cursor App
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Cursor App Response
        run: |
          echo "🎯 Cursor GitHub App responding..."
          echo "Repository: ${{ github.repository }}"
          echo "Event: ${{ github.event_name }}"
          echo "Action: ${{ github.event.action }}"
          
          # This is what Cursor needs to see for the app to work
          echo "✅ Cursor GitHub App is active and responding"
